# 《新面试题解答_0320》

> 题目来源：`D:/google-downloads/自编面试题_0316开始.md`
>
> 说明：
> 1. 下面的问题标题尽量按原题保留，不再沿用上一版题纲。
> 2. 每题先给 **【面试当场如何回答】**，格式统一为：
>    - `通用答法`
>    - `结合我这个项目`
> 3. 后面再给 **【详细解释】**，用于你自己理解、应对追问。
> 4. 所有项目口径优先以当前工作区可复核的 `YAML / runner / MCP / 训练产物 / 评测报告` 为准。

---

## 面试前统一口径

### 你可以先背的 30 秒项目总述

这个项目不是简单聊天 demo，而是一个 `Router + 专家 Agent + MCP 工具层 + 结构化记忆 + Hybrid RAG + LoRA + 离线评测` 的多智能体旅游助手。  
我主要做了四件事：第一，把系统从广播式改成 `travel_router` 集中路由；第二，把天气、景点、路线能力做成 MCP 工具，降低事实幻觉；第三，做了本地 Hybrid RAG，把景点知识库扩到 `15723` 条；第四，基于 LLaMA-Factory 做了 `router_lora` 和 `spot_lora`，其中 `router_lora` 的完整链路成功率从 `20.59%` 提升到 `85.29%`。

### 面试里建议优先说的硬指标

- 知识库规模：`15723` 条
- Router 训练数据：`833` 条
- Spot 训练数据：`8802` 条
- Router benchmark：`34` 条
- Spot internal benchmark：`80` 条
- CrossWOZ benchmark：`150` 条
- RAG benchmark：`198` 条
- Router 完整链路成功率：`20.59% -> 85.29%`
- Router turn exact：`48.81% -> 94.05%`
- Router argument exact：`19.57% -> 89.13%`

### 面试里要稳着说的 4 个点

1. `spot_lora` 不是“全面变好”，而是**表达风格增强，但 strict factual 指标下降**。
2. 当前 RAG 不是“标准 BM25 + cross-encoder rerank”，而是 `FAISS + BM25-like + handwritten bonus` 的工程化混合排序。
3. 当前没有独立 cross-encoder reranker。
4. RAG 主报告来自 `2026-03-13` 的 `15240` 条旧库版本；当前运行库已经扩到 `15723` 条，所以要说成“**报告基于旧版本，运行库已继续扩充**”。

---

# Agent项目相关问题

## 1. 怎么解决幻觉，输出慢是什么原因以及如何解决？先说常见的方案，再举例说明你这个项目是怎么做的。

### 【面试当场如何回答】

- **通用答法**：我会先把幻觉分成两类，一类是事实型幻觉，一类是行为型幻觉。事实型幻觉一般靠工具调用、RAG、检索增强和输出约束解决；行为型幻觉一般靠角色拆分、权限隔离、流程约束和评测闭环解决。输出慢通常来自长上下文、模型解码、外部工具、检索和多步编排。
- **结合我这个项目**：我这个项目主要用了四层方法压幻觉：天气和路线这类强事实问题必须走工具；`travel_router` 和专家 Agent 做权限隔离；景点问题用 `高德 POI + 本地 RAG` 做 grounding；再用 Router tool-calling benchmark、Spot benchmark 和 RAG 检索评测去找 badcase。速度方面，我主要从结构化记忆、减少无效检索、懒加载索引和缩短链路这些方向优化。

### 【详细解释】

#### 1.1 幻觉为什么会出现

- **事实型幻觉**：本来应该查工具或知识库的数据，模型直接编。
- **行为型幻觉**：本来只该路由的 Agent 越权自己查业务工具，或者该发消息却吐纯文本。

#### 1.2 常见解决思路

| 问题类型 | 常见方案 |
| --- | --- |
| 事实型幻觉 | Function Calling、外部工具、RAG、检索增强、输出格式约束 |
| 行为型幻觉 | 多 Agent 角色拆分、权限隔离、状态机/Prompt 约束、benchmark + badcase |

#### 1.3 我这个项目怎么做

1. **高风险事实问题强制走工具**
   - 天气：`get_weather` / `get_weather_forecast`
   - 路线：`get_driving_route`
2. **权限隔离**
   - `travel_router` 只做 `get_context / save_context / send_direct_message`
   - `weather_agent / spot_agent / plan_agent` 才能做业务工具调用
3. **景点场景用 Hybrid RAG**
   - 高德 POI 负责实时地点信息
   - 本地 RAG 负责攻略、避坑、玩法
4. **评测闭环**
   - Router：tool-calling benchmark
   - Spot：strict success / fact coverage / ROUGE-L
   - RAG：Hit@K / MRR

#### 1.4 输出慢的来源

可以把总时延近似拆成：

`T_total ≈ T_prefill + T_decode + T_tool + T_network + T_retrieval + T_orchestration`

| 慢的来源 | 项目里的表现 | 优化思路 |
| --- | --- | --- |
| 上下文太长 | 历史消息越多，prefill 越慢 | 用结构化记忆替代整段原文重放 |
| 外部 API | 天气和 POI 都有网络请求 | 缓存热点城市、减少重复查 |
| RAG 检索 | FAISS + embedding + 融合排序 | 懒加载、缓存热门查询 |
| 多 Agent 编排 | Router 要先分发，专家再执行 | 简单问题走最短路径 |

---

## 2. agent间数据怎么流动【节点间怎么运行的】，你的项目里边举个例子？比如travel_router 是如何进行意图分发，用户提问天气问题后交给weather agent的？消息路由机制具体怎么做的？

### 【面试当场如何回答】

- **通用答法**：多 Agent 的数据流一般包括三层：自然语言消息流、结构化状态流、工具结果流。最常见的机制是“统一入口识别意图，再把任务路由给对应专家节点”。
- **结合我这个项目**：我这个项目不是广播抢答，而是 `Router + Direct Message 专家 Agent`。用户先在公共频道发消息，`travel_router` 监听公共频道，先 `get_context`，做指代消解和意图识别，然后通过 `send_direct_message(target_agent_id="weather_agent", text=...)` 转给 `weather_agent`。`weather_agent` 只监听来自 Router 的 direct message，再执行 `get_context -> get_weather -> send_channel_message`。

### 【详细解释】

#### 2.1 运行机制

| 环节 | 谁负责 | 事件类型 |
| --- | --- | --- |
| 公共频道入口 | `travel_router` | `thread.channel_message.notification`、`thread.reply.notification` |
| 定向执行 | `weather_agent / spot_agent / plan_agent` | `thread.direct_message.notification` |

#### 2.2 三类数据在流动

1. **自然语言任务数据**：用户原问题、Router 转发后的文本。
2. **结构化状态数据**：`current_city`、`current_spot`、`preferences` 等。
3. **工具返回事实数据**：天气、POI、路线结果。

#### 2.3 天气问题的完整链路

```text
用户在 general 发问
-> travel_router 收到消息
-> get_context 读取记忆
-> 判断为天气意图
-> send_direct_message 给 weather_agent
-> 视情况 save_context(current_city=...)
-> weather_agent 收到 direct message
-> get_context 补全上下文
-> get_weather(city=...)
-> send_channel_message 回 general
```

#### 2.4 当前消息路由机制为什么能工作

- Router 和专家 Agent 的 trigger 不同
- Router 有发送者过滤，忽略来自其他 Agent 的消息
- 专家 Agent 只处理来自 Router 的 direct message

这避免了“Agent 听到别的 Agent 回复又重新处理”的回声循环。

---

## 3. 你提到lora微调，这个lora微调和直接修改提示词、RAG 这两种工作有什么效果上的区别？你怎么弄的测试指标衡量lora效果？ 你是做lora微调，还是就学了个lora原理呢、讲讲lora原理？超参数的数值如何确定，列出你lora时候的超参数选定。然后全参训练、qlora和 lora的 区别 优劣 选择？你项目中lora训练取哪个step？我看你有个lora取得不是final而是中间一个checkpoint 为什么？数据处理/参数选择 step选择 损失分析 这一块 就是你是有一个评价指标，你的目的是提升模型在这上面的性能 有一个目标，针对这个目标你做了啥。lora为什么rank要设置成8，16这样的数，数字大小除了对显存占用有影响，对模型效果的影响真的这么大吗？为什么alpha大家都是设置成rank的两倍，这个的原因是什么呢，原论文不是相等吗？还有不同的target_module在模型微调中具体起了什么作用。加在qkvo这几个上有什么区别。 你的lora数据集是怎么制作的？lora微调的badcase分析有没有？lora为啥省显存？

### 【面试当场如何回答】

- **通用答法**：Prompt、RAG、LoRA 解决的问题不一样。Prompt 是把规则告诉模型，RAG 是补外部知识，LoRA 是把角色行为和输出分布更稳定地学进参数。LoRA 的核心是冻结大部分原模型参数，只训练一个低秩增量，所以显存和训练成本都比全参小。
- **结合我这个项目**：我不是只学了 LoRA 原理，而是实际做了两条 LoRA：`router_lora` 和 `spot_lora`。`router_lora` 的目标是提升路由和工具调用稳定性，评测用的是 tool-name accuracy、argument exact match、turn exact match、sample full-chain success；结果里完整链路成功率从 `20.59%` 提升到 `85.29%`。`spot_lora` 更偏回答风格增强。超参数上两条线都用了 `r=8, alpha=16, dropout=0.05`，Router 学习率 `5e-5`，Spot 学习率 `3e-5`。Router 最后取的是 `checkpoint-60`，因为下游 benchmark 比 final 更好。

### 【详细解释】

#### 3.1 Prompt、RAG、LoRA 的区别

| 方案 | 改什么 | 解决什么问题 | 局限 |
| --- | --- | --- | --- |
| Prompt | 输入条件和规则 | 告诉模型该怎么做 | 模型不一定真的学住 |
| RAG | 外部知识输入 | 补知识、降知识幻觉 | 不直接解决角色边界 |
| LoRA | 模型参数增量 | 稳定角色行为、格式、动作分布 | 数据和指标不对齐时也会学偏 |

你可以直接背一句：

**Prompt 像制度，RAG 像查资料，LoRA 像把工作习惯训练进参数。**

#### 3.2 LoRA 原理

LoRA 的核心写法：

`W' = W0 + ΔW`

`ΔW = (alpha / r) * B A`

其中：

- `W0`：原始权重
- `A`、`B`：低秩矩阵
- `r`：rank
- `alpha`：缩放系数

为什么省显存：

1. 不更新原模型大参数，只训练小的 adapter 参数。
2. 优化器状态只为 adapter 保存，不为全模型保存。
3. 存储时只需要保存 adapter，而不是整套模型权重。

#### 3.3 LoRA / QLoRA / 全参训练怎么比较

| 方案 | 优点 | 缺点 | 适合什么场景 |
| --- | --- | --- | --- |
| 全参微调 | 容量最大、可塑性最强 | 显存高、训练慢、易破坏通用能力 | 大资源、任务边界明确 |
| LoRA | 显存友好、训练快、adapter 可插拔 | 容量有限 | 角色化微调、快速迭代 |
| QLoRA | 在 LoRA 基础上再量化底座，更省显存 | 训练链路更复杂、量化细节要注意 | 显存更紧张的场景 |

当前项目为什么优先 LoRA：

- 任务是角色化行为适配，不是全面重塑模型
- 需要分角色做 adapter
- 需要快速迭代 benchmark

#### 3.4 当前项目里我实际做了什么

- `router_lora`
  - 目标：意图识别、路由、参数构造、记忆顺序
- `spot_lora`
  - 目标：景点/攻略回答风格、表达组织

所以我不是“只会讲原理”，而是完整做了：

1. 数据准备
2. 数据格式转换
3. LLaMA-Factory 训练
4. checkpoint 选择
5. 离线评测
6. badcase 分析

#### 3.5 当前可复核超参数

| 项目 | `router_lora` | `spot_lora` |
| --- | --- | --- |
| base model | `Qwen3.5-4B` | `Qwen3.5-4B` |
| `lora_rank` | `8` | `8` |
| `lora_alpha` | `16` | `16` |
| `lora_dropout` | `0.05` | `0.05` |
| `learning_rate` | `5e-5` | `3e-5` |
| `cutoff_len` | `2048` | `1536` |
| `num_train_epochs` | `1.5` | `1.0` |
| `gradient_accumulation_steps` | `8` | `8` |
| `val_size` | `0.1` | `0.05` |
| `template` | `qwen3_5_nothink` | `qwen3_5_nothink` |

#### 3.6 为什么 `rank` 常从 8、16 开始

- 不是玄学
- 是容量、泛化、显存之间的工程折中
- `r` 太小容易学不动，`r` 太大又不一定继续提升

`r` 影响的不只是显存，也影响模型能学到多复杂的更新子空间。  
新增参数量近似：

`Params_LoRA ≈ r * (d_in + d_out)`

#### 3.7 为什么很多人设 `alpha = 2r`

- 关键不是看 `alpha` 单独多大，而是看 `alpha / r`
- `alpha = 2r` 相当于 `scale = 2`
- 这是工程上常见的稳妥经验，不是必须遵守的定律

更严谨的说法是：

**原论文不是规定永远相等；后续工程实践里，很多人发现 `alpha / r` 设成 1 到 2 比较稳，所以 `2r` 变成常见习惯。**

#### 3.8 target module 和 `q/k/v/o`

| 模块 | 直观含义 | 改它意味着什么 |
| --- | --- | --- |
| `q_proj` | 我想关注什么 | 改关注点、提问方式 |
| `k_proj` | 别人怎么被我匹配到 | 改可匹配性 |
| `v_proj` | 我取回什么内容 | 改被聚合的信息表示 |
| `o_proj` | 多头结果怎么合并 | 改汇总后的输出组织 |

当前项目里 `lora_target = all`，说明不只是改 `q/k/v/o`，还改了更多线性层。

#### 3.9 LoRA 数据集怎么做

| 数据线 | 样本量 | 目标 |
| --- | ---: | --- |
| Router | `833` | 工具调用轨迹学习 |
| Spot | `8802` | 景点/文旅问答与攻略表达 |

Router 数据处理链路：

1. 先生成 ShareGPT 风格 `gpt + tool_calls`
2. 再转成 LLaMA-Factory 用的 `function_call + observation`
3. 训练后如果要给 LM Studio 推理，再转回 `gpt + tool_calls`

#### 3.10 我怎么评估 LoRA

`router_lora` 重点看动作类指标：

- Tool-name accuracy
- Argument exact match
- Turn exact match
- Sample full-chain success

原始结果：

| 指标 | Base | Router LoRA |
| --- | ---: | ---: |
| Tool-name accuracy | `51.19%` | `100.00%` |
| Argument exact match | `19.57%` | `89.13%` |
| Turn exact match | `48.81%` | `94.05%` |
| Sample full-chain success | `20.59%` | `85.29%` |

#### 3.11 为什么有的 checkpoint 不是 final

- loss 最低不等于下游业务指标最好
- Router 真正关心的是工具选对和参数填对
- 所以 Router 选中间的 `checkpoint-60`

一句话：

**checkpoint 不是看谁训练得最久，而是看谁在目标 benchmark 上最好。**

#### 3.12 badcase 分析有没有

有。

- Router：从“经常动作错”收敛到“少数字段漏填”
- Spot：暴露出“风格增强但 strict factual 不稳”

---

## 4. 加了个东西，反而导致模型效果变差了是什么原因？我看你这个spot lora有个指标还差了一些 那就分析不足，为什么没别人的好，哪里出了问题，数据量太少，还是数据质量太差，还是超参，还是模型结构太简单还是太复杂，欠拟合还是过拟合，改进思路是什么？

### 【面试当场如何回答】

- **通用答法**：模型加了新东西后变差，通常不是一句“训练失败”能解释，要区分训练目标和评测目标是否一致、数据分布是否匹配、模型是不是把容易优化的部分学得太重了。
- **结合我这个项目**：`spot_lora` 不是全面变差，而是出现了结构性变化。公开评测里 `ROUGE-L F1` 明显上升，说明更像旅游助手；但 `Strict success` 和 `Fact coverage` 下降，说明它更会说了，但不一定更像严格槽位抽取器。根本原因是训练目标、数据分布和评测指标没完全对齐。后续改进方向是把“事实抽取”和“自然表达”拆开。

### 【详细解释】

#### 4.1 原始结果怎么解读

| 指标 | Base | Spot LoRA | 结论 |
| --- | ---: | ---: | --- |
| Strict success | `98.00%` | `78.00%` | 下降 |
| Fact coverage | `99.00%` | `89.11%` | 下降 |
| ROUGE-L F1 | `2.66%` | `27.36%` | 明显上升 |

#### 4.2 为什么会这样

1. **训练目标和评测目标不一致**
   - SFT 优化 token 概率
   - strict success 看的是离散槽位全对
2. **数据分布更偏攻略表达**
   - `spot` 数据更像“会写旅游话术”
   - 不完全是“字段抽取数据”
3. **任务本身太杂**
   - 同时想要检索理解、事实抽取、自然表达

#### 4.3 这到底算过拟合还是欠拟合

不要简单二选一。更准确的说法是：

- 对风格分布来说，学得有点重
- 对结构化精度来说，约束又不够强

所以更像：

**风格过拟合 + 槽位约束不足**

#### 4.4 改进思路

| 改法 | 作用 |
| --- | --- |
| 先结构化再生成 | 先出字段，再渲染自然语言 |
| 补 strict-format 数据 | 强化门票、时长、名称等关键槽位 |
| 明确字段覆盖模板 | 没信息也要输出“未查到” |
| facts / style 分开做 adapter | 避免一个 adapter 同时扛两类目标 |

---

## 5. RAG你怎么实现的？rag系统有问题，一般怎么排查？怎么解决？我看你改了好几版RAG，出于什么原因和目的？如何切分chunk、固定还是自适应？rerank用没用？怎么融合的BM25和另一个检索？有没有评估hit rate、hallucination？embedding模型选型？embedding的意义在哪里？提示：和one-hot相比。如何搭建的RAG数据库？你用什么指标来衡量的，具体？ 你提到是hybrid RAG，是什么和什么混合？ 远程API和本地RAG的优先级和调用先后顺序？RAG的核心是你的召回率、准确率怎么样，你怎么优化的。提升了多少。你有没有效果指标。

### 【面试当场如何回答】

- **通用答法**：一个完整 RAG 系统通常要讲四件事：知识库怎么建、检索怎么做、排序怎么做、怎么评估和排查。排查时一般按 `query 解析 -> 召回 -> 排序 -> 生成 -> 数据源` 五层定位。
- **结合我这个项目**：我这个项目不是单一向量库，而是 `高德 POI + 本地 FAISS + BM25-like` 的 Hybrid RAG。本地库目前是 `15723` 条，embedding 用 `BAAI/bge-small-zh-v1.5`，索引用 `FAISS IndexFlatIP`，查询时再叠加 BM25-like 词法分和手写攻略 bonus。当前 chunk 不是自适应切分，而是“一条结构化景点记录就是一个检索单元”；也没有独立 cross-encoder rerank。评测上我做了 `Hit@1/3/5` 和 `MRR`，现成报告显示城市级问题较稳，但精确景点 strict 检索还是短板。

### 【详细解释】

#### 5.1 什么是这个项目里的 Hybrid RAG

| 层级 | 混合了什么 |
| --- | --- |
| 系统层 | 本地 RAG + 高德 POI API |
| 本地检索层 | dense 检索 + BM25-like lexical + handwritten bonus |

#### 5.2 数据库怎么搭

- 当前运行库：`15723` 条
- 主要来源：
  - Kaggle/去哪儿网：`14581`
  - CrossWOZ：`465`
  - 独家手写攻略：`347`
  - China312：`312`
  - 香港旅游 QA：`18`

知识库记录核心 schema：

- `id`
- `city`
- `district`
- `spot_name`
- `tags`
- `content`
- `duration`
- `budget`
- `rating`
- `source`

#### 5.3 embedding 为什么重要，和 one-hot 有什么区别

| 表示方式 | 特点 | 适合当前项目吗 |
| --- | --- | --- |
| one-hot / bag-of-words | 稀疏、只看词面、无语义相似性 | 不适合问法多变的旅游问答 |
| embedding | 稠密、能表达语义相近 | 适合做语义召回 |

当前 embedding 选型：

- 模型：`BAAI/bge-small-zh-v1.5`
- 原因：中文效果好、体量适中、本地 CPU 也能跑

#### 5.4 chunk 怎么切

当前最真实的说法：

- 一条结构化景点/攻略记录就是一个 chunk
- 不是 token 滑窗切块
- 也不是语义自适应切分

优点：

- 城市、景点名、标签不会被切碎
- 结构完整、好解释

缺点：

- 长文本的局部精准定位能力有限

#### 5.5 检索和融合怎么做

当前融合公式可近似写成：

`score(d, q) = 5 * dense_score + bm25_like_score + handwritten_bonus`

其中：

- `dense_score`：FAISS top-5 向量召回分
- `bm25_like_score`：查询时现算的启发式词法分
- `handwritten_bonus`：手写攻略命中时额外加权

#### 5.6 BM25 和“另一个检索”怎么融合

“另一个检索”就是 dense retrieval，也就是 embedding + FAISS。  
所以这里的融合不是“两个标准检索器”，而是：

- 语义召回分
- 词法补分
- 手写攻略 bonus

#### 5.7 rerank 用没用

严格说：

- **没有独立 cross-encoder reranker**
- 当前更像工程化融合排序

#### 5.8 远程 API 和本地 RAG 的优先级

稳妥说法不是“永远先谁后谁”，而是：

- 高德 POI：负责实时地点信息
- 本地 RAG：负责攻略、避坑、玩法
- 当前 `spot_agent` 往往会综合两路结果回答

#### 5.9 怎么排查 RAG

我会按五层定位：

1. query 有没有被错改写
2. 召回 topK 里有没有目标
3. 排序有没有把好结果压下去
4. 检索到了但生成没说对
5. 数据源本身有没有

#### 5.10 怎么评估

当前离线指标：

- `Hit@1 / Hit@3 / Hit@5`
- `MRR`
- 严格 Doc-ID 口径
- 宽松实体级口径

现成主报告结果基于 `15240` 条旧库版本：

| 口径 | Hit@1 | Hit@3 | Hit@5 | MRR |
| --- | ---: | ---: | ---: | ---: |
| 严格 Doc-ID | `35.9%` | `36.9%` | `36.9%` | `0.3636` |
| 宽松实体级 | `42.9%` | `43.9%` | `43.9%` | `0.4343` |

结论：

- 手写攻略很强
- 城市级问题较稳
- 精确景点 strict 检索仍是短板

#### 5.11 hallucination 有没有单独指标

严格说没有独立统一的 hallucination 总表。  
当前是通过三组指标间接看：

1. RAG 检索指标
2. Spot 回答指标
3. 人工 badcase 复盘

---

## 6. 你如果json格式输出错了怎么办？

### 【面试当场如何回答】

- **通用答法**：我不会只答“让模型重试”。更工程化的做法是优先避免自由 JSON，改用 Function Calling；如果必须输出 JSON，就走 schema 约束、解析校验、repair、重试/回退这一整套容错链路。
- **结合我这个项目**：我这个项目在线链路已经尽量不让模型直接手写高风险 JSON，而是优先走 MCP + Function Calling。离线数据清洗阶段如果有脏 JSON，可以修，但不会把“看起来像 JSON”的结果直接拿去执行高风险动作。

### 【详细解释】

自由 JSON 常见问题：

- 少逗号
- 引号不匹配
- 键名漂移
- 字段漏填

更稳的流程：

`生成 -> 解析 -> 校验 -> 修复/重试 -> 决策执行`

| 层 | 处理方式 |
| --- | --- |
| schema 层 | 约束字段名、类型、必填项 |
| parser 层 | `json.loads` 或等价解析 |
| validator 层 | schema validate |
| recovery 层 | repair / retry / fallback |

---

## 7. 你这个项目说是多智能体，体现在哪里？为什么要做多智能体，而不是一个大模型直接做完？多agent的四种范式是什么？多agent 比单agent 有什么优势？

### 【面试当场如何回答】

- **通用答法**：多智能体的核心不是“起了几个名字”，而是有角色分工、权限边界和消息协作。相比单 Agent，多 Agent 更容易控边界、拆评测、做局部优化。
- **结合我这个项目**：我这个项目里 `travel_router` 做总控，`weather_agent / spot_agent / plan_agent` 做专责执行，之间通过公共频道和 direct message 协作，这就不是一个 prompt 里假装多个身份，而是真实的多 Agent 协作。当前主要属于 `Router / Hub-and-Spoke` 范式。

### 【详细解释】

| 角色 | 当前职责 |
| --- | --- |
| `travel_router` | 路由、记忆读写、指代消解 |
| `weather_agent` | 天气查询 |
| `spot_agent` | 景点/攻略 |
| `plan_agent` | 路线和行程 |

多 Agent 四种常见范式：

1. `Router / Hub-and-Spoke`
2. `Planner-Executor`
3. `Blackboard / Shared Memory`
4. `Debate / Negotiation`

单 Agent vs 多 Agent：

| 维度 | 多 Agent | 单 Agent |
| --- | --- | --- |
| 边界 | 更清晰 | 容易混 |
| 权限控制 | 容易隔离 | 容易越权 |
| 问题定位 | 好拆 | 容易糊成一团 |
| 扩展 | 易加专家角色 | 易变得很重 |
| 延迟 | 更高 | 更低 |

---

## 8. 这个项目的提示词工程/优化做了什么工作？

### 【面试当场如何回答】

- **通用答法**：Prompt 工程不只是“润色回答”，更重要的是把角色边界、动作顺序、输出格式写成可执行协议。
- **结合我这个项目**：Router 的 prompt 重点是先 `get_context`、做指代消解、再分发并必要时 `save_context`；Weather 的重点是必须走真实天气工具；Spot 的重点是把 POI 和本地 RAG 的使用边界写清楚；Plan 的重点是路线和行程专责化。Prompt 负责写制度，LoRA 负责把制度学稳。

### 【详细解释】

| Agent | Prompt 重点 |
| --- | --- |
| Router | 先读记忆、再消解、再分发、再保存状态 |
| Weather | 必须走天气工具，不跨域 |
| Spot | 检索策略显式化 |
| Plan | 路线和行程专责化 |

一句话：

**这个项目里的 Prompt 工程，本质是在给多 Agent 写“行为操作系统”。**

---

## 9. 你这个项目中提到了MCP，怎么做的？有什么作用？

### 【面试当场如何回答】

- **通用答法**：MCP 可以理解为把外部能力标准化成模型可调用工具接口的一层协议，重点价值是统一接入、权限隔离和可扩展。
- **结合我这个项目**：我现在有两个 MCP Server。`memory_mcp.py` 只暴露 `save_context/get_context` 给 Router；`mcp_server.py` 暴露天气、景点、本地 RAG、路线等工具给专家 Agent。这样 Router 不能越权碰业务工具，专家 Agent 也能用标准接口调外部能力。

### 【详细解释】

| MCP Server | 工具 | 给谁用 |
| --- | --- | --- |
| `memory_mcp.py` | `save_context`、`get_context` | `travel_router` |
| `mcp_server.py` | `get_weather`、`get_weather_forecast`、`search_spots`、`search_local_knowledge`、`search_combined`、`get_driving_route` | 专家 Agent |

一句很适合面试的话：

**MCP 不是让模型更聪明，而是让模型更容易接系统。**

---

## 10. 是否了解多模态Agent解决方案？

### 【面试当场如何回答】

- **通用答法**：多模态 Agent 的关键不是“能收图片”这么简单，而是能把不同模态先转成结构化可用信息，再接入决策链路。
- **结合我这个项目**：当前项目主线还是文本多 Agent，不是完整多模态系统，所以我不会硬说已经做成了多模态。但它的 Router + 专家架构非常适合扩多模态，比如前面加 `Vision/OCR Agent` 或 `ASR Agent`，先把图片、语音转成文本和实体，再交给现有专家链路。

### 【详细解释】

旅游场景里的多模态用例：

1. 拍景点图片问“这是哪里，值不值得去”
2. 拍车票/订单/路牌截图问“怎么安排”
3. 拍菜单或特产问“这是什么，推荐买吗”

可行链路：

```text
图片/语音输入
-> Vision/OCR/ASR Agent
-> 抽取文本和实体
-> travel_router 编排
-> spot_agent / plan_agent / weather_agent 执行
```

---

## 11. 你用的openagents框架，讲讲这个框架的大致特点？为什么不用langchain？你了解langchain嘛？

### 【面试当场如何回答】

- **通用答法**：选框架不是比谁“更高级”，而是看和场景匹不匹配。消息驱动多 Agent 和状态图工作流，其实更适合不同风格的框架。
- **结合我这个项目**：我这个项目当前更适合 OpenAgents，因为它是 YAML 声明式、多角色、事件驱动、workspace messaging、MCP 接入都比较顺手。LangChain / LangGraph 我也了解，如果后续项目更强调确定性的状态图和 DAG 编排，LangGraph 会是很合理的选择。

### 【详细解释】

| 维度 | OpenAgents | LangChain / LangGraph |
| --- | --- | --- |
| 当前项目匹配点 | 多角色、消息驱动、事件触发 | 强确定性图编排更强 |
| 配置方式 | YAML 声明式 | 代码式拼装更多 |
| 多 Agent 协作 | 很顺手 | 也能做，但组织方式不同 |
| 工具接入 | MCP 直连 | Tools / Retriever / Graph 节点 |

稳妥口径：

**我了解 LangChain 的 PromptTemplate、Tools、Retriever、AgentExecutor，也了解 LangGraph 的图式编排思路，但这个项目更像消息驱动的多 Agent 协作，所以当前优先选了 OpenAgents。**

---

## 12. 记忆模块怎么做，为什么不直接把所有历史对话全塞进去？

### 【面试当场如何回答】

- **通用答法**：记忆一般有两种做法，一种是原文重放，一种是结构化状态记忆。不是所有历史都适合塞进模型，因为成本高、噪声大，而且很多信息本质上是 state。
- **结合我这个项目**：我当前做的是结构化状态记忆，把 `current_city`、`current_spot`、`preferences`、`trip_days` 这类长期有价值的信息存到本地 `storage/user_context.json`。Router 和专家 Agent 都先 `get_context` 再处理。这样既能续上下文，又不会每轮都把所有原文塞进去。

### 【详细解释】

当前核心字段：

- `current_city`
- `origin_city`
- `destination_city`
- `current_spot`
- `travel_party`
- `preferences`
- `departure_time`
- `trip_days`
- `notes`
- `last_updated`

为什么不全塞历史：

| 原因 | 解释 |
| --- | --- |
| 成本高 | 自注意力复杂度近似 `O(n^2)` |
| 噪声大 | 很多历史对当前问题没用 |
| 状态更适合结构化 | 城市、偏好、天数天然就是 state |

当前不足也要主动说：

**它现在还是单文件全局记忆，不是按用户/线程隔离的生产级记忆层。**

---

## 13. Function Calling  是怎么回事？你这个项目提到了Function Calling ，具体是什么 讲讲？

### 【面试当场如何回答】

- **通用答法**：Function Calling 的本质不是“模型会写函数”，而是让模型从“说话”转成“做动作”，输出结构化调用意图，让系统执行对应工具。
- **结合我这个项目**：我这个项目里 Router、Weather、Spot、Plan 都在做不同形式的 Function Calling。Router 要决定是 `get_context`、`send_direct_message` 还是 `save_context`；Weather 要决定什么时候调 `get_weather`；Spot 要决定查 POI 还是查本地 RAG。

### 【详细解释】

普通文本生成 vs Function Calling：

| 方式 | 模型做什么 |
| --- | --- |
| 普通生成 | 直接给自然语言答案 |
| Function Calling | 先输出“调哪个函数、带什么参数” |

当前项目里的动作链：

| Agent | 核心动作链 |
| --- | --- |
| Router | `get_context -> send_direct_message / send_channel_message -> save_context` |
| Weather | `get_context -> get_weather -> send_channel_message` |
| Spot | `get_context -> search_spots / search_local_knowledge / search_combined -> send_channel_message` |
| Plan | `get_context -> get_driving_route -> send_channel_message` |

一句话：

**MCP 是把工具接进来，Function Calling 是让模型选对工具和参数。**

---

## 14. sft这样的垂域模型做微调的意义和价值在哪里？

### 【面试当场如何回答】

- **通用答法**：SFT 的价值不在于替代知识库和工具，而在于把角色、流程、格式和风格更稳定地学进参数。
- **结合我这个项目**：Router LoRA 的明显提升就是最好的证据，它让模型从“知道规则”变成“更稳定执行规则”；Spot LoRA 也说明了另一面，如果数据和评测目标没对齐，SFT 也可能把模型往“更会说但不一定更准”的方向推。

### 【详细解释】

工具、RAG、SFT 的分工：

| 组件 | 解决什么问题 |
| --- | --- |
| 工具 | 实时外部事实 |
| RAG | 外部知识检索 |
| SFT / LoRA | 稳定使用这些能力的行为模式 |

所以这个项目里，SFT 最有价值的地方不是“背旅游知识”，而是更稳地：

- 正确路由
- 正确调工具
- 正确组织结果

---

## 15. 你提到基于LLama factory去做的lora微调，这个是什么框架还是工具？

### 【面试当场如何回答】

- **通用答法**：LLaMA-Factory 更准确地说是一个面向大模型微调和推理评测的训练工具链，不是模型本身。
- **结合我这个项目**：我主要用它做了两件事，一是跑 `router_lora` 和 `spot_lora` 的训练，二是把训练产物、日志、checkpoint 和离线评测串起来。它解决的是训练流程问题，不替你解决数据质量和评测可信度。

### 【详细解释】

当前项目里，它的体现主要是：

- `training_args.yaml`
- `trainer_state.json`
- `adapter_config.json`
- `checkpoint-*`

它为什么适合当前项目：

- 任务是角色化 SFT / LoRA
- 需要看 checkpoint
- 需要本地离线评测

---

## 16. 你的Agent能处理复杂的问题吗？

### 【面试当场如何回答】

- **通用答法**：复杂问题能不能处理，要先定义“复杂”是多轮澄清、多约束条件，还是开放式长程任务。通常复杂问题靠任务拆解、记忆、工具和状态管理来处理，而不是一句 prompt 解决。
- **结合我这个项目**：我这个项目现在能处理的是**中等复杂度、结构相对明确**的问题，比如多轮补全城市、带偏好约束的景点推荐、路线和天气这类组合场景。但它还不适合特别开放的长程规划，因为当前主要是单 Router 分发，专家之间没有复杂协商，记忆也还是结构化摘要而不是完整任务状态机。

### 【详细解释】

你可以把“复杂问题”拆成三档：

| 复杂度 | 例子 | 当前项目情况 |
| --- | --- | --- |
| 低 | 单轮问天气、单轮问景点 | 稳定支持 |
| 中 | 多轮补城市、带偏好推荐、路线+行程 | 有一定支持 |
| 高 | 开放式长程旅行策划、反复权衡预算/时间/天气 | 还不够强 |

当前项目能处理复杂问题，主要依赖：

1. Router 做指代消解
2. 结构化记忆保留关键状态
3. 专家 Agent 调工具拿事实

但它的上限也很明显：

- 还不是任务级规划器
- 没有多 Agent 协商
- 没有真正的 long-horizon task graph

---

## 17. 你的Agent的对话可以存储到本地吗方便下次继续吗，如何实现的？

### 【面试当场如何回答】

- **通用答法**：本地持久化一般有三种做法：原始对话日志、结构化记忆、向量化长期记忆。要不要支持“下次继续”，关键看存的是原文、摘要还是状态。
- **结合我这个项目**：我这个项目当前确实支持本地存储，但主要是**结构化状态记忆**，不是完整对话全文回放。底层是 `tools/memory_tools.py` 把 `current_city`、`current_spot`、`preferences` 等字段写到 `storage/user_context.json`，下次处理前由 `get_context()` 读出来继续用。

### 【详细解释】

当前实现链路：

```text
Router / 专家 Agent
-> 调用 save_context
-> tools/memory_tools.py
-> 写入 storage/user_context.json
-> 下次处理时 get_context 读取
```

所以它能做到的是：

- 记住当前城市
- 记住当前景点
- 记住偏好、行程天数等

但它还做不到：

- 完整多用户会话隔离
- 完整原始对话轨迹回放
- 基于 thread 的长会话恢复

稳妥说法：

**当前已经能“部分续接”，但续的是结构化上下文，不是完整聊天历史。**

---

## 18. 为什么没做RL，是不懂吗？

### 【面试当场如何回答】

- **通用答法**：不是所有问题都该优先上 RL。RL 更适合 reward 难直接标注、需要长期策略优化的场景，但代价是奖励设计复杂、训练不稳定、成本高。
- **结合我这个项目**：我这个项目当前优先级更高的是把 SFT、工具调用、RAG 和离线 benchmark 先做扎实。因为 Router 这类任务已经有清晰监督数据和明确指标，先用 SFT/LoRA 是性价比最高的，不是因为我不懂 RL，而是当前阶段上 RL 收益不一定最高。

### 【详细解释】

为什么当前没优先做 RL：

1. **监督信号已经很清楚**
   - Router 是否选对工具、参数是否填对，都可以直接评测
2. **reward 设计并不简单**
   - 多 Agent 链路里，奖励分配给哪个动作、哪个 Agent 都是难点
3. **训练成本更高**
   - RL 往往比 SFT 更贵、更不稳定
4. **当前阶段瓶颈更偏工程稳定性**
   - 比如 memory、RAG、工具调用边界、benchmark 可信度

如果以后真要上 RL，更合适的方向是：

- Router 动作链成功率做 reward
- 或者先做 DPO / 偏好优化，再考虑更强 RL

---

## 19. 你的Agent能不能将对话外化用于用户观看呢？

### 【面试当场如何回答】

- **通用答法**：可以。对话外化通常有三层：用户可见聊天记录、工具调用轨迹、内部状态流转时间线。做不做取决于你想给用户看多少、要不要做审计和可解释性。
- **结合我这个项目**：我这个项目当前已经把用户最终可见的对话外化到了公共频道消息层，用户能直接看到专家 Agent 的回复。如果要继续增强，也可以把 Router 分发、工具调用结果和记忆变化做成一个 trace 面板给用户看。但当前 `master` 这版还没有单独做完整的可视化 trace dashboard。

### 【详细解释】

外化可以分三层：

| 层级 | 给谁看 | 当前项目情况 |
| --- | --- | --- |
| 聊天层 | 用户 | 已有，用户能看到频道回复 |
| 工具层 | 开发/审计 | 可通过日志和轨迹拿到 |
| 状态层 | 研发/高级用户 | 暂未做成独立可视化面板 |

如果以后要做得更完整，可以展示：

- Router 识别出的意图
- 分发到了哪个 Agent
- 调了哪个工具
- 记忆字段怎么更新

但要注意：

- 不该暴露模型思维链
- 用户层展示要以动作轨迹和状态变化为主

---

## 20. schema？

### 【面试当场如何回答】

- **通用答法**：schema 本质上就是数据或接口的结构契约，约束字段名、类型、必填项和语义边界。没有 schema，系统就容易出现字段漂移和上下游对不齐。
- **结合我这个项目**：我这个项目里至少有三类 schema：第一类是记忆 schema，比如 `current_city / current_spot / preferences`；第二类是景点知识库 schema，比如 `city / district / spot_name / tags / content`；第三类是工具调用 schema，也就是函数名和参数结构。当前没有一个统一 schema registry，但这三类 schema 已经在代码和数据里隐式落地了。

### 【详细解释】

| schema 类型 | 当前项目示例 | 作用 |
| --- | --- | --- |
| 记忆 schema | `current_city`、`trip_days`、`preferences` | 统一长期状态 |
| 知识库 schema | `id`、`city`、`district`、`spot_name`、`tags`、`content` | 统一建库和检索 |
| 工具调用 schema | `get_weather(city=...)`、`send_direct_message(target_agent_id=..., text=...)` | 统一动作接口 |

为什么 schema 重要：

1. 防止字段漂移
2. 让训练、推理、评测口径一致
3. 便于做校验和容错

如果老师继续问“你现在 schema 做到什么程度”，最稳妥的答法是：

**当前是分模块 schema，不是统一注册中心；够用，但还可以继续工程化。**

---

## 21. 总结一下你这个agent项目全流程和不足之处。

### 【面试当场如何回答】

- **通用答法**：一个 Agent 项目全流程，通常要讲入口、路由、状态、工具、知识库、模型适配、评测和迭代修复这几层。不足之处最好主动讲，不要只报喜。
- **结合我这个项目**：我这个项目的全流程是：用户在公共频道提问，`travel_router` 先读记忆、做指代消解和意图识别，再把任务发给天气/景点/路线专家；专家 Agent 调 MCP 工具拿事实，必要时结合本地 RAG，再把结果回复到公共频道。模型层我又做了 Router 和 Spot 两条 LoRA；最后通过 Router、Spot、RAG 三套离线评测和 badcase 持续迭代。当前不足主要是记忆还没按用户/线程隔离，RAG 还没有独立 reranker，复杂长程任务处理也还不够强。

### 【详细解释】

#### 21.1 全流程

```text
用户发消息
-> travel_router 监听公共频道
-> get_context 读取结构化记忆
-> 指代消解 + 意图识别
-> send_direct_message 给目标专家
-> 专家 Agent 调 MCP 工具
-> 景点问题可结合本地 RAG
-> send_channel_message 回公共频道
-> save_context 更新状态
-> benchmark + badcase 继续迭代
```

#### 21.2 这个项目真正有价值的点

1. 不是单一 prompt demo，而是完整系统链路
2. 有 LoRA、RAG、MCP、记忆、评测闭环
3. 有可复核指标，不是纯主观体验

#### 21.3 当前不足

| 不足 | 具体表现 |
| --- | --- |
| 记忆层 | 仍是单文件全局记忆，不是用户/线程隔离 |
| RAG | 没有独立 cross-encoder rerank |
| 多 Agent | 还是 Router 单点分发，没有复杂协商 |
| 长程任务 | 复杂开放任务处理能力有限 |
| 可视化 | 没有完整 trace dashboard |

---

## 22. 聊聊未来规划什么的。AI时代应该具体怎么做？

### 【面试当场如何回答】

- **通用答法**：AI 时代不能只会调 API，也不能只停在理论，要把“模型、数据、系统、评测”四条线一起补齐。真正有竞争力的是能把抽象能力落到具体场景里。
- **结合我这个项目**：对我来说，短期是把 Agent/RAG/工具调用这套工程链再做扎实，比如用户隔离记忆、RAG rerank、多模态接入、trace 可视化；中期希望把这些工程问题抽成更系统的研究问题，比如记忆、检索和行为稳定性的结合。AI 时代我理解最重要的不是追热点，而是持续做可复核、可落地、可迭代的系统。

### 【详细解释】

你可以把“AI 时代怎么做”说成四件事：

1. **补基础**
   - 数学、概率、深度学习、系统基础
2. **补工程**
   - 数据、训练、评测、部署、监控
3. **补场景**
   - 找具体垂域，把需求抽清楚
4. **补判断力**
   - 知道什么时候该用 Prompt、RAG、SFT、RL

结合你这个项目，未来可以继续做：

- 用户/线程隔离记忆
- 更强的多意图 Router
- 独立 reranker
- 多模态 Agent
- 用户可见 trace

---

# NLP、Transformer项目相关问题

## 1. 讲讲transformer大概内容原理。相比CNN\RNN突出的特点。另外说说现在还用得到Transformer吗？GPT是基于它么？ transformer为什么能处理不同长度的序列？

### 【面试当场如何回答】

- **通用答法**：Transformer 是基于 self-attention 的序列建模架构，用 attention 替代了 RNN 的串行递归结构。相比 RNN，它更容易并行、长距离依赖更容易建；相比 CNN，它不依赖固定局部卷积核，任意位置都能直接交互。现在当然还在用，GPT 就是 Decoder-only Transformer。它能处理不同长度序列，是因为 attention 和 mask 机制本身支持可变长度输入，只要位置编码和 padding mask 处理好就行。
- **结合我这个项目**：在我的 NLP 项目里，不管是阅读理解里用的 ERNIE / RoBERTa，还是现在大模型底座 Qwen，本质上都还是 Transformer 系列。也就是说，这不是过时结构，而是目前主流大模型和很多下游 NLP 模型的共同基础。

### 【详细解释】

#### 1.1 Transformer 核心

最标准的一句话：

**Transformer 用 self-attention 替代循环结构，让序列中任意位置都能直接交互。**

#### 1.2 Transformer vs CNN vs RNN

| 模型 | 优点 | 局限 |
| --- | --- | --- |
| CNN | 局部模式提取强、并行好 | 长距离依赖不自然 |
| RNN/LSTM | 适合顺序建模 | 串行计算，长依赖难 |
| Transformer | 并行强、长依赖好、扩展性强 | 长序列注意力成本高 |

#### 1.3 GPT 是不是基于它

是。  
GPT 属于 **Decoder-only Transformer**。

#### 1.4 为什么能处理不同长度序列

核心原因：

1. 输入是 token 序列，不要求固定长度
2. attention 本身支持变长
3. 用 padding mask 屏蔽补齐位
4. 用位置编码补顺序信息

---

## 2. 机器学习的一点内容，比如如何降维什么的，还有pytorch paddlepaddle精华内容你了解多少，然后就是当时项目里各个分类模型的一些八股。 

### 【面试当场如何回答】

- **通用答法**：这类题我会按“基础概念 + 一个典型方法 + 一个项目例子”来答。降维我优先讲 PCA；PyTorch 我重点讲 `nn.Module / autograd / Dataset-DataLoader / optimizer`；Paddle 我会讲静态图部署和 PaddleNLP 生态。分类模型八股我会从传统机器学习、CNN/RNN 到预训练模型三层来讲。
- **结合我这个项目**：我的阅读理解项目主要还是 PaddlePaddle / PaddleNLP 生态，抽取式 QA 本质还是 Transformer 编码 + QA Head；Agent 项目则更多是 PyTorch 生态周边和大模型工程。也就是说我不是只懂一个框架，而是知道不同项目该选什么工具链。

### 【详细解释】

#### 2.1 如何降维

| 方法 | 类型 | 一句话理解 |
| --- | --- | --- |
| PCA | 线性降维 | 找方差最大的正交方向 |
| 特征选择 | 不是变换，是筛特征 | 直接减少维度 |
| AutoEncoder | 非线性降维 | 用编码器学低维表示 |
| t-SNE / UMAP | 可视化为主 | 更适合展示，不适合直接训练 |

PCA 最稳妥的一句话：

**PCA 把原始特征投影到方差最大的几个正交方向上，用更少维度保留主要信息。**

#### 2.2 PyTorch / Paddle 该怎么答

| 框架 | 你可以重点说什么 |
| --- | --- |
| PyTorch | 动态图、`nn.Module`、`autograd`、`Dataset/DataLoader`、optimizer |
| Paddle | PaddleNLP、`paddle.jit`、静态图部署、Paddle Inference |

#### 2.3 分类模型八股怎么讲

| 层级 | 代表模型 | 适用点 |
| --- | --- | --- |
| 传统 ML | LR、SVM、XGBoost | 特征明确、小数据 |
| 深度模型 | TextCNN、BiLSTM | 序列建模、局部模式 |
| 预训练模型 | BERT / ERNIE / RoBERTa | 语义表示最强，迁移能力好 |

---

## 3. 讲解一下attention机制？还有别的计算注意力的方式吗？attention中K,V维度不同该怎么办？padding，裁剪，维度变换？多头自注意力机制如何理解？

### 【面试当场如何回答】

- **通用答法**：Attention 的本质是用 `Q` 和 `K` 做匹配，再用匹配权重对 `V` 做加权汇总。标准公式是 `Attention(Q, K, V) = softmax(QK^T / sqrt(dk))V`。Padding 要靠 mask；Q 和 K 的最后一维必须能对齐做点积，V 的 value 维可以不同；多头自注意力就是让模型在不同子空间并行看不同关系。
- **结合我这个项目**：这套机制既是我 NLP 项目里 ERNIE / RoBERTa 的核心，也是我 Agent 项目底座模型 Qwen 的核心。LoRA 常加在 `q/k/v/o` 上，本质上就是在调注意力里“看什么、怎么匹配、取回什么、怎么汇总”的关键位置。

### 【详细解释】

标准公式：

`Q = XWQ`  
`K = XWK`  
`V = XWV`

`Attention(Q, K, V) = softmax(QK^T / sqrt(dk))V`

多头注意力：

`head_i = Attention(XWQ_i, XWK_i, XWV_i)`

#### 常见变体

| 类型 | 特点 |
| --- | --- |
| 加性注意力 | 用小网络打分 |
| 余弦注意力 | 用余弦相似度匹配 |
| 稀疏注意力 | 只算局部或稀疏连接 |
| 线性注意力 | 通过近似降低复杂度 |
| MQA / GQA | 共享或分组共享 K/V |

#### K、V 维度不同怎么办

要区分两件事：

1. **序列长度**
   - `len(Q)` 可以和 `len(K), len(V)` 不同
   - 但 `K` 和 `V` 的序列长度要一致
2. **特征维度**
   - `Q` 和 `K` 的最后一维要一致，才能点积
   - `V` 的 value 维可以不同

#### padding、裁剪、维度变换

- padding：用 mask 把 padding 位变成 `-inf`
- 裁剪：优先保留最近上下文和关键实体
- 维度变换：线性层把 `d_model` 投影到每个 head 的 `dk/dv`

---

## 4. resnet v1和v2的区别，resnet和normalization解决梯度消失，二者的区别，还有残差块放relu前还是后比较好？

### 【面试当场如何回答】

- **通用答法**：ResNet 的核心是残差连接，让网络学残差而不是直接学完整映射，解决深层退化和梯度传播困难。ResNet v1 是 `Conv-BN-ReLU` 的后激活结构，ResNet v2 是 `BN-ReLU-Conv` 的前激活结构。Normalization 稳定的是特征分布，残差连接改善的是信息和梯度的直通路径，两者不是替代关系。
- **结合我这个项目**：我当前主项目不是视觉模型，但这个问题我会按“结构差异 + 为什么更稳 + 和梯度消失的关系”来答，不会只背一个结论。

### 【详细解释】

| 维度 | ResNet v1 | ResNet v2 |
| --- | --- | --- |
| 激活位置 | 后激活 | 前激活 |
| 典型顺序 | `Conv-BN-ReLU` | `BN-ReLU-Conv` |
| shortcut 通过性 | 较弱 | 更顺滑 |
| 深层训练稳定性 | 好 | 通常更好 |

Residual 和 Normalization 的区别：

| 机制 | 主要解决什么 |
| --- | --- |
| 残差连接 | 让梯度和信息有更短路径 |
| Normalization | 稳定激活分布、提升训练稳定性 |

“ReLU 放前还是后”：

- 浅层网络差异不一定大
- 深层网络里，**前激活通常更稳**

---

## 5. 你提到了各自策略：adamW/lamb优化器、softmax交叉熵损失/focal loss损失函数、FGM/AWP对抗训练 你都知道这是什么原理，作用于模型哪些层吗？

### 【面试当场如何回答】

- **通用答法**：AdamW 和 Lamb 都是 Adam 系优化器，但 Lamb 额外做逐层自适应 trust ratio，更适合大 batch；softmax 交叉熵适合互斥分类，focal loss 适合强调困难样本；FGM 是在 embedding 上加扰动，AWP 是在权重上加扰动。
- **结合我这个项目**：在我的阅读理解项目里，最终实验上 AdamW 明显优于 Lamb，softmax 交叉熵优于 focal loss，FGM 优于 AWP。也就是说我不只是知道原理，还做过实际对比，最后选的是 `AdamW + softmax + FGM`。

### 【详细解释】

#### 5.1 AdamW vs Lamb

| 优化器 | 核心特点 | 适合什么 |
| --- | --- | --- |
| AdamW | Adam + 解耦权重衰减 | Transformer 微调、小中 batch |
| Lamb | AdamW + trust ratio | 大 batch 训练 |

项目里实验结果：

| 优化器 | F1 | EM |
| --- | ---: | ---: |
| AdamW | `87.34` | `74.45` |
| Lamb | `81.72` | `67.81` |

#### 5.2 softmax 交叉熵 vs focal loss

| 损失 | 适合什么场景 |
| --- | --- |
| softmax 交叉熵 | 互斥分类、位置竞争 |
| focal loss | 类别极不平衡、想强调难样本 |

项目里结果：

- softmax 明显优于 focal
- 原因是阅读理解答案位置本质上更像“互斥位置选择”

#### 5.3 FGM vs AWP

| 方法 | 扰动对象 | 作用层 |
| --- | --- | --- |
| FGM | Embedding 输入 | 嵌入层 |
| AWP | 模型权重 | 多个 weight 参数 |

项目里结果：

- FGM 比 AWP 稳定
- FGM 实现简单、超参更稳

---

## 6. 你用的ernie3.0xbase 架构怎么样？有多少个transformer块？和BERT的区别是什么？BERT有多少个？可以列出表格对比。

### 【面试当场如何回答】

- **通用答法**：`ERNIE-3.0-xbase-zh` 本质上也是 Transformer 编码器架构，只是预训练设计和规模比 BERT base 更强。它在我的项目里是 `20` 层 Transformer Encoder，`hidden size=1024`，`16` 个 attention heads。
- **结合我这个项目**：在我的阅读理解项目里，最终加载方式是 `AutoModelForQuestionAnswering.from_pretrained("ernie-3.0-xbase-zh")`，也就是说主干结构没有自己手改，而是在原生 `ERNIE-3.0-xbase-zh + QA Head` 上做微调。和 BERT 相比，它不是“完全不同范式”，而是同属 Transformer 编码器家族，但预训练和规模更强。

### 【详细解释】

#### 6.1 项目中可复核的 ERNIE-3.0-xbase-zh 配置

| 项目 | ERNIE-3.0-xbase-zh |
| --- | --- |
| Transformer blocks | `20` |
| hidden size | `1024` |
| attention heads | `16` |
| params | `296M` |
| 任务头 | QA Head |

#### 6.2 和 BERT 的对比

| 维度 | ERNIE-3.0-xbase-zh | BERT-base-chinese |
| --- | --- | --- |
| 架构类型 | Encoder-only Transformer | Encoder-only Transformer |
| 层数 | `20` | `12` |
| hidden size | `1024` | `768` |
| attention heads | `16` | `12` |
| 参数规模 | `296M` | 约 `110M` |
| 预训练设计 | ERNIE 系更强调知识增强和更丰富任务 | 经典 MLM + NSP |

一句话：

**它和 BERT 不是两条完全不同路线，而是同家族里的更强版本。**

---

## 7. 你提到了用paddle.jit 静态图转动态图，这是什么？

### 【面试当场如何回答】

- **通用答法**：这个表述要先纠正一下，更准确地说通常是“动态图转静态图”，不是“静态图转动态图”。`paddle.jit` 的核心作用是把动态图模型导出成更适合部署的静态图形式。
- **结合我这个项目**：我在阅读理解项目里用 `paddle.jit.save` 导出 `.pdmodel / .pdiparams / .pdiparams.info`，部署时再用 `paddle.jit.load` 加载。这样做的好处是推理更快、更适合部署。我实际用的是 `paddle.jit.load`，不是单独的 Paddle Inference C++ 引擎。

### 【详细解释】

| API | 作用 |
| --- | --- |
| `paddle.jit.save(model)` | 导出静态图模型 |
| `paddle.jit.load()` | 加载静态图模型 |

为什么静态图更适合部署：

- 算子融合
- 常量折叠
- 内存优化
- 执行更高效

---

## 8. F1分数和EM分数是什么？还有什么常见的评测指标？

### 【面试当场如何回答】

- **通用答法**：`EM` 是 Exact Match，要求预测答案和标准答案完全一致；`F1` 是精确率和召回率的调和平均，更关注部分重叠。阅读理解里一般同时报 `F1` 和 `EM`，因为一个看严格完全一致，一个看局部覆盖。
- **结合我这个项目**：在我的中医阅读理解项目里，`F1` 和 `EM` 都是核心指标；在 Agent 项目里，我又扩展到了 `Hit@K`、`MRR`、tool-calling exact match 这类更贴近系统行为的指标，所以我理解指标要和任务对齐，不能只会背 `F1/EM`。

### 【详细解释】

#### 8.1 EM

`EM = 1` 当且仅当预测答案和标准答案完全一致，否则为 `0`。

#### 8.2 F1

`F1 = 2 * precision * recall / (precision + recall)`

在抽取式问答里：

- `precision = overlap / pred`
- `recall = overlap / gold`

#### 8.3 常见指标

| 任务 | 常见指标 |
| --- | --- |
| 分类 | Accuracy、Precision、Recall、F1、AUC |
| 阅读理解 | F1、EM |
| 检索 | Hit@K、MRR、Recall@K、NDCG |
| 生成 | ROUGE、BLEU、BERTScore |
| Agent / 工具调用 | Tool-name accuracy、Argument exact match、Turn exact match、Success rate |

---

## 速记对比表

### 1. Prompt / RAG / LoRA / 工具

| 能力 | 解决什么问题 |
| --- | --- |
| Prompt | 把规则告诉模型 |
| RAG | 补外部知识 |
| 工具 | 拿实时事实 |
| LoRA / SFT | 把角色行为和格式更稳地学进参数 |

### 2. MCP / Function Calling

| 概念 | 一句话理解 |
| --- | --- |
| MCP | 把工具接入系统 |
| Function Calling | 让模型选对工具和参数 |

### 3. LoRA / QLoRA / 全参

| 方案 | 优点 | 缺点 |
| --- | --- | --- |
| LoRA | 快、便宜、可插拔 | 容量有限 |
| QLoRA | 更省显存 | 链路更复杂 |
| 全参微调 | 可塑性最强 | 成本最高 |

### 4. 多 Agent / 单 Agent

| 维度 | 多 Agent | 单 Agent |
| --- | --- | --- |
| 边界 | 清晰 | 容易混 |
| 权限控制 | 容易隔离 | 容易越权 |
| 评测拆分 | 容易 | 较难 |
| 延迟 | 更高 | 更低 |

### 5. OpenAgents / LangGraph

| 维度 | OpenAgents | LangGraph |
| --- | --- | --- |
| 更适合 | 消息驱动多 Agent | 强确定性状态图 |
| 当前项目匹配度 | 高 | 也能做，但不是当前首选 |

---

## 补充：Qwen3.5-4B 与本地部署（结合本项目实际训练）

### 1. Qwen3.5-4B 在我这个项目里到底承担什么角色？

### 【面试当场如何回答】

- **通用答法**：`Qwen3.5-4B` 在我这里不是“随便挑了个底座”，而是整个 LoRA 训练和本地部署的统一基座。它的价值不是参数最大，而是比较适合中文 Agent 场景，能兼顾指令遵循、工具调用和本地部署成本。
- **结合我这个项目**：我这个项目里，`router_lora` 和 `spot_lora` 都是基于 `Qwen3.5-4B` 训练出来的。`router_lora` 主要学意图识别、子 Agent 分发、参数构造和记忆读写顺序；`spot_lora` 主要学景点介绍、推荐表达和旅游助手式回答组织。所以它在我项目里既是训练底座，也是部署底座。

### 【详细解释】

从我项目里的真实训练配置看，`Qwen3.5-4B` 不是只被“拿来跑推理”，而是被真正用于两条 LoRA 训练线：

| 训练线 | 真实配置 | 任务目标 |
| --- | --- | --- |
| `router_lora_2026-03-12` | `bf16=true`、`template=qwen3_5_nothink`、`cutoff_len=2048`、`lr=5e-5`、`epoch=1.5`、`batch=2`、`grad_acc=8`、`rank=8`、`alpha=16`、`dropout=0.05`、`scheduler=cosine`、`warmup=10`、`eval/save_steps=20`、`val_size=0.1` | 学会 `get_context -> send_direct_message -> save_context` 这一类多 Agent 工具链动作 |
| `spot_lora_2026-03-12` | `bf16=true`、`template=qwen3_5_nothink`、`cutoff_len=1536`、`lr=3e-5`、`epoch=1.0`、`batch=2`、`grad_acc=8`、`rank=8`、`alpha=16`、`dropout=0.05`、`scheduler=cosine`、`warmup=30`、`eval/save_steps=50`、`val_size=0.05` | 学会景点讲解、攻略组织、旅游助手式表达 |

这两条训练线最后的结果也不一样：

- `router_lora` 最终选的是 `checkpoint-60`
- `spot_lora` 最终选的是 `final / checkpoint-523`

这也说明我不是“只训练一个 LoRA 然后到处套”，而是按任务拆开训。

另外，你这次项目里一个非常关键的点是：**不是只改 `q/k/v/o`，而是用了 `lora_target=all`。**

从 `adapter_config.json` 里能直接看到，这次不是只打在 `qkvo` 上，而是实际命中了 **16 个 target modules**。`router_lora` 和 `spot_lora` 命中的模块集合基本一致，包括：

| 类别 | 这个项目里实际命中的层 |
| --- | --- |
| Attention 相关 | `q_proj`、`k_proj`、`v_proj`、`o_proj`、`qkv`、`out_proj`、`attn.proj`、`in_proj_qkv` |
| MLP / FFN 相关 | `gate_proj`、`up_proj`、`down_proj`、`linear_fc1`、`linear_fc2` |
| Qwen3.5 特有投影层 | `in_proj_a`、`in_proj_b`、`in_proj_z` |

所以你面试里要说得更准确一点：

- 不是“我只在 qkvo 上做了个轻量 LoRA”
- 而是“我在 `Qwen3.5-4B` 上用了 `lora_target=all`，实际覆盖了 attention、FFN/MLP 和 Qwen3.5 特有投影层”

这样说才和你自己的训练文档一致。

为什么这里要用 `all`，而不是只改 `qkvo`？

- `router` 不只是改注意力方向，它还要学 **工具调用顺序、参数格式、记忆读写流程**
- `spot` 不只是改“关注哪里”，它还要学 **内容组织、旅游表达风格、回答结构**

所以你这次项目的任务，不是单纯 attention 调整，覆盖 MLP/FFN 是合理的。

---

### 2. 为什么我最后选 Qwen3.5-4B 做本地部署？

### 【面试当场如何回答】

- **通用答法**：本地选模型我主要看 4 件事：能不能在我的机器上跑起来、工具调用稳不稳、中文场景效果够不够、部署链路顺不顺。
- **结合我这个项目**：我项目记录里其实做过一轮真实取舍。更大的模型在本地太慢，`Qwen2.5` 虽然更快，但我当时的主观测试感受是“更像给方案，不太积极执行工具”。最后 `Qwen3.5-4B + LM Studio + /no_think` 是我这台本地机器上最能兼顾速度、工具调用和稳定性的方案。

### 【详细解释】

这里不要回答成“因为它最强”，要回答成“因为它最适合我的机器和系统”。

从你自己的项目记录里，真实背景是：

- 你本地部署目标是 `LM Studio`
- 你明确提到过 `GTX 1650Ti 4GB` 这一类低显存约束
- 你尝试过更大的 `Qwen` 系列模型，也尝试过 `Qwen2.5`
- 最后结论是：**`Qwen3.5-4B` 更适合作为你当前旅游 Agent 系统的本地基座**

可以整理成这个对比：

| 你项目里的真实取舍 | 现象 | 最终判断 |
| --- | --- | --- |
| 更大的 `8B/14B` 路线 | 本地太慢，响应和稳定性都不理想 | 不适合你当前机器 |
| `Qwen2.5` 路线 | 速度快，但你自己的记录里认为“只给方案不执行”，工具感偏弱 | 不作为当前首选 |
| `Qwen3.5-4B` | 在 `LM Studio` 下响应、工具调用和 no_think 控制更平衡 | 成为当前主方案 |

再结合当前 `master` 分支的真实配置，可以更具体地说：

| Agent | 当前模型名 | 含义 |
| --- | --- | --- |
| `travel_router` | `router-qwen35-lora` | Router 单独吃一套路由 LoRA |
| `weather_agent` | `spot-qwen35-lora` | 当前和业务侧共享同一套业务模型 |
| `spot_agent` | `spot-qwen35-lora` | 景点/攻略回答主模型 |
| `plan_agent` | `spot-qwen35-lora` | 当前也复用业务模型 |

而且它们都不是走云 API，而是统一走：

- `provider = openai`
- `api_base = http://localhost:1234/v1`
- `api_key = lm-studio`

也就是说，你当前项目已经不是“抽象地说本地部署”，而是：

- 训练侧：`Qwen3.5-4B` 做 LoRA 基座
- 部署侧：通过 `LM Studio` 暴露本地 OpenAI 兼容接口
- 运行侧：`router` 和业务专家模型已经分成两个本地模型 ID

一句话总结：

**我选 `Qwen3.5-4B`，是因为它在我的机器上能真正落地，而且既能撑住 Router 的工具路由，也能给 Spot 线提供可训练的中文生成底座。**

---

### 3. 你在 LM Studio 里跑的是量化版、LoRA 版，还是什么？

### 【面试当场如何回答】

- **通用答法**：这个问题一定要分训练侧和部署侧。训练侧是 Hugging Face 基座模型加 LoRA adapter；部署侧如果进 `LM Studio`，一般走的是 `GGUF` 量化链路。
- **结合我这个项目**：我项目里的真实链路是这样的：训练时我在 `LLaMA-Factory` 上用 `Qwen3.5-4B` base 做 `bf16` 的 LoRA 微调；部署文档里明确写过 `LM Studio` 采用 `Qwen3.5-4B-GGUF-Q4_K_M.gguf` 这一类量化模型；当前 `master` 运行时则通过 `http://localhost:1234/v1` 调用 `LM Studio` 暴露出来的 `router-qwen35-lora` 和 `spot-qwen35-lora` 这两个模型 ID。

### 【详细解释】

这个问题你最好按“训练、导出、部署、运行”四层来答：

| 阶段 | 你项目里的实际证据 | 应该怎么说 |
| --- | --- | --- |
| 训练侧 | `training_args.yaml` 里是 `bf16=true`，`model_name_or_path=/root/autodl-tmp/models/Qwen3.5-4B` | 训练时用的是 Hugging Face 格式的 `Qwen3.5-4B` 基座 |
| Adapter 侧 | `models/router_lora_2026-03-12`、`models/spot_lora_2026-03-12` | 我训练出了两套 LoRA adapter |
| 导出侧 | `models/merge_lora.py` 里明确写了“下一步：转换为 GGUF 格式以便 LM Studio 加载” | 说明 LoRA 不是直接扔给 LM Studio，而是要 merge / export |
| 部署侧 | `implementation_plan.md` 里明确写过 `Qwen3.5-4B-GGUF-Q4_K_M.gguf` | 部署链路是 GGUF 量化思路，而且文档里实际提到过 `Q4_K_M` |
| 运行侧 | `.env` 和各 agent YAML 都指向 `http://localhost:1234/v1` | OpenAgents 最终是通过 LM Studio 的本地 OpenAI 接口调用模型 |

所以最严谨的口径是：

- **训练时**：`Qwen3.5-4B` base + LoRA adapter，属于 Hugging Face / LLaMA-Factory 训练链路
- **部署时**：走 `GGUF` 量化链路给 `LM Studio`
- **运行时**：OpenAgents 通过 `localhost:1234/v1` 去调 `LM Studio`

你还可以再补一句更像你项目实际的话：

> 我这个项目不是单纯“拿一个 GGUF 裸聊”，而是先在 `Qwen3.5-4B` 上训出 `router_lora` 和 `spot_lora`，再走本地部署链路。训练配置里两个 LoRA 都是 `lora_target=all`，所以我优化的不只是 qkvo，而是 attention、MLP 和 Qwen3.5 特有投影层。部署时再把这套能力落到 `LM Studio` 的本地服务里。

如果面试官继续追问“那你现在到底在跑什么”，你可以直接这样答：

> 训练时我跑的是 `Qwen3.5-4B` 的 bf16 基座加 LoRA；部署到 `LM Studio` 时走的是 `GGUF` 量化链路，项目文档里明确写过 `Q4_K_M`；当前 `master` 里的 OpenAgents 通过 `localhost:1234/v1` 调的是 `router-qwen35-lora` 和 `spot-qwen35-lora` 这两个本地模型入口。

---

## 最后提醒

1. `Router LoRA` 是你最硬的证据，优先讲。
2. `spot_lora` 要说“风格增强，但 strict factual 指标下降”，不要硬说全面提升。
3. `RAG` 不要吹成“标准 BM25 + rerank”，要说“FAISS + BM25-like + bonus 的 Hybrid RAG”。
4. 记忆模块要主动补一句：**当前还是单文件全局记忆，不是按用户/线程隔离。**
5. 面试里最加分的不是全都说成最强，而是你能主动区分：
   - 什么是当前真实实现
   - 什么是已经验证有效
   - 什么是当前识别出的技术债和下一步改进
